---
# Bake workflow — thin Dagger wrapper.

name: Bake

on:  # yamllint disable-line rule:truthy
  workflow_dispatch:
    inputs:
      source:
        description: 'Source to bake'
        required: true
        default: 'ambientcg'
        type: choice
        options:
          - ambientcg
          - polyhaven
          - gpuopen
          - physicallybased
      tier:
        description: 'Resolution tier'
        required: true
        default: '1k'
        type: choice
        options:
          - 128
          - 256
          - 512
          - 1k
          - 2k
      limit:
        description: 'Max materials (0 = all)'
        required: false
        default: '0'
        type: string
      offset:
        description: 'Skip first N materials (for chunked execution)'
        required: false
        default: '0'
        type: string
      batch-size:
        description: 'Materials per streaming batch'
        required: false
        default: '50'
        type: string
      upload-chunks:
        description: 'Upload + delete each parquet partition as it closes (frees runner disk)'
        required: false
        default: true
        type: boolean
      release-tag:
        description: 'Calver data release tag (e.g. v2026.04.0)'
        required: false
        default: 'v2026.04.0'
        type: string

permissions:
  contents: write

jobs:
  bake:
    name: Bake ${{ inputs.source }} ${{ inputs.tier }}
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4

      - uses: dagger/dagger-for-github@v8.4.1
        with:
          verb: call
          module: .dagger
          args: >-
            bake-and-release
            --src=.
            --source=${{ inputs.source }}
            --tier=${{ inputs.tier }}
            --limit=${{ inputs.limit }}
            --offset=${{ inputs.offset }}
            --batch-size=${{ inputs.batch-size }}
            --upload-chunks=${{ inputs.upload-chunks }}
            --release-tag=${{ inputs.release-tag }}
            --registry-pass=env:GITHUB_TOKEN
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
